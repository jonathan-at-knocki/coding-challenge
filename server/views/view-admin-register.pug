extends view-admin-submit_creds

append variables
  - var title = 'Register new admin user';
  - var actionLoc = '/admin/register';
  - var submitText = 'Add user';

//-block preerror
//-  div
//-    a(href="/admin/login") login with existing user
//-