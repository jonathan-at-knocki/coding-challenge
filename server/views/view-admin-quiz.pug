//- the view for a single user's quiz in the admin panel

extends view-admin-base

include _includes/show_quiz

append variables
  - var title = 'Quiz for ' + quiz.startIp;
  - bodyClass += " admin-quiz";

block content
  a.link-admin-main(href="/admin") Back
  +showQuiz
    //- block for last question
    span.current-q
